version: 2.1

commands:
  print_command:
    steps:
      - run: 
          name: on error
          command: echo "Hello this is command"


jobs:
  my_job:
    docker:
      - image: circleci/node:13.8.0
    steps:
    - print_command

workflows:
  my_workflow:
    jobs:
      - my_job
