# env variables
version: 2.1

workflows:
  test-env-vars:
    jobs:
      - build
         requires:
           - list_file

jobs:
  build:
    docker:
      - image: cimg/base:2020.01
    steps:
      - checkout
      - run:
          name: "echo an env var that is part of our project"
          command: |
            touch test-file
            ls
  list_file:
    docker:
      - image: circleci/node:13.8.0
    steps:
      - run: cat test-file
